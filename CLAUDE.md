# Claude Repo Rules

Follow [AGENTS.md](AGENTS.md) as the primary repository policy.

Claude-specific expectation:

- Do not use `/Volumes/devssd/orchestrator` as an edit workspace for ordinary work.
- Reserve an operator worktree first.
- If an active self-repair feature exists, assume it owns the overlapping runtime surface unless the user explicitly says to intervene.
